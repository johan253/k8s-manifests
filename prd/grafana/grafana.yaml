apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: helm-grafana
  namespace: monitoring
  labels:
    dashboards: "grafana"
spec:
  external:
    url: http://grafana.prd.johanhernandez.com
    apiKey:
      name: grafana-operator-token
      key: GRAFANA_TOKEN
